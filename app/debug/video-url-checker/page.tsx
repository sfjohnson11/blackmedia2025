export const dynamic = "force-dynamic";

export default function VideoUrlCheckerPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-2">Video URL Checker</h1>
      <p className="text-gray-400">
        Temporarily simplified to fix a build error. We can restore the full tool later.
      </p>
    </div>
  );
}
